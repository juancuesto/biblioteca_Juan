# Nombre de la aplicación
spring.application.name=biblioteca_Juan

# Configuración de la fuente de datos
spring.datasource.url=jdbc:postgresql://bbdd.jprcdev.com:5432/biblioteca
spring.datasource.username=jruizcaisa
spring.datasource.password=Colorestu22
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA e Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop

# Mostrar las consultas SQL en la consola
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración del pool de conexiones (opcional)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000
